/**
 * 原生接口
 */

let InvokeTypes = {
  func: {
    login: 'func.login',
    back: 'func.goBack',
    ready: 'func.ready',
    openAppPage: 'func.openAppPage',
    share: 'func.share'
  }
};

export default InvokeTypes;
